import styled from '@emotion/styled';

export const ActionCell = styled.div(() => ({
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    gap: '20px',
    button: {
        width: 'max-content'
    }
}));
