import styled from '@emotion/styled';

export const TableWrapper = styled.div({
    marginTop: '40px',

    table: {
        width: '100%',
        fontSize: '16px',

        '& td': {
            padding: '20px 0'
        }
    }
});
